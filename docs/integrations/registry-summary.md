# Registry Summary Helper

Run this before publishing the MCP registry metadata:

```bash
node scripts/registry-summary.mjs
```

This prints a compact summary of `registry.server.json` so you can confirm the package identifier, MCP name, and transport before submitting.
