{
  "name": "substack-cli-codex",
  "version": "0.1.0",
  "description": "Codex integration docs for the Substack CLI MCP server",
  "private": true,
  "mcpServers": {
    "substack-cli": {
      "command": "npx",
      "args": [
        "-y",
        "@edithatogo/substack-cli",
        "mcp",
        "serve"
      ],
      "env": {
        "SUBSTACK_PUBLICATION_URL": "https://your-publication.substack.com"
      }
    }
  }
}
