{
  "name": "substack-cli-vscode",
  "displayName": "Substack CLI MCP Helper",
  "description": "Launch and configure the Substack CLI MCP server from VS Code.",
  "version": "0.1.0",
  "publisher": "edithatogo",
  "engines": {
    "vscode": ">=1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "contributes": {
    "configuration": {
      "title": "Substack CLI MCP Helper",
      "properties": {
        "substackCli.mcpCommand": {
          "type": "string",
          "default": "npx -y @edithatogo/substack-publisher mcp serve",
          "description": "Command used to launch the local Substack CLI MCP server."
        },
        "substackCli.publicationUrl": {
          "type": "string",
          "default": "https://your-publication.substack.com",
          "description": "Publication URL placeholder passed to the MCP server as SUBSTACK_PUBLICATION_URL."
        }
      }
    }
  }
}
