---
name: conductor
description: Use Conductor context in this repository. Start at conductor/index.md for tracks, specs, and plans. Use when the user mentions Conductor, tracks, conductor-implement, or programme work.
---

# Conductor (repo pointer)

Do not re-vendor the Conductor plugin here. The maintainer already has Conductor skills installed.

## Before changing product code

1. Read `conductor/index.md` and follow its links.
2. Resolve the active track from `conductor/tracks.md` or `conductor/tracks/`.
3. Read that track’s `spec.md` / `plan.md` / `metadata.json` only after the path is verified.
4. Follow `conductor/workflow.md`. Mark tasks `[~]` while in progress and `[x]` only with evidence.
5. Leave `conductor/github-programme.json` and Project #38 nesting to the programme agent unless this task owns them.

## Codex / Cursor

If a full Conductor skill is available on the machine (for example under `~/.codex/skills/conductor/`), follow that protocol. This file only binds agents to this repo’s `conductor/` artifacts.
