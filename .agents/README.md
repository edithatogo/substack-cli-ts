Conductor skills are not re-vendored in this repository.

Use `.cursor/skills/conductor/SKILL.md` plus `conductor/index.md`. If a machine-level Conductor plugin is already installed, follow that protocol against this repo’s `conductor/` artifacts.
